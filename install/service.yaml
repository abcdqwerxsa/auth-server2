apiVersion: v1
kind: Service
metadata:
  name: oauth-server
  namespace: fuyao-oauth
spec:
  ports:
    - port: 9096
      protocol: TCP
      targetPort: 9096
      nodePort: 30096
  selector:
    application: "oauth-server"
  type: NodePort