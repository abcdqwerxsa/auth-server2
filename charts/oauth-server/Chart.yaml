apiVersion: v2
name: oauth-server
description: >
  A Helm chart for openFuyao OAuth2.0 server. It interacts with console-service to provide access token for each user. Users are required to input passwords for authentication.

type: application
version: 0.14.1
appVersion: "dev-migrate-registry"
keywords: ["openfuyao-component"]
dependencies: []
